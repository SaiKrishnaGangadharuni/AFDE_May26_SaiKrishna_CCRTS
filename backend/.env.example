# Copy this file to backend/.env and adjust as needed.

# Override the SQLite location or switch to PostgreSQL by uncommenting:
# DATABASE_URL=postgresql://user:pass@localhost:5432/ccrts

# Generate a long random string for production:
SECRET_KEY=change-me-in-prod-please-use-a-long-random-string
ACCESS_TOKEN_EXPIRE_MINUTES=480
